---
name: domains-ip-infrastructure-subdomains-index
description: 'Group index for tools/domains-ip-infrastructure/subdomains: one-line accounting of children.'
kind: group-index
---

# domains-ip-infrastructure/subdomains

## Skills here
- **`altdns`** (low) — Use when you have a set of known subdomains for a `domain` and want to discover more by permutation — returns resolved alternate subdomains.
- **`aquatone`** (low) — Use when you have a `domain` (or a list of discovered hosts) and want a fast visual map of its web attack surface — returns HTTP screenshots
- **`bluto`** (low) — Use when you have a `domain` and want an all-in-one recon pass (DNS records, zone-transfer/brute subdomains, and harvested staff emails) — r
- **`dns-recon`** (low) — Use when you have a `domain` and want its full DNS footprint — returns records, discovered subdomains, zone-transfer and PTR results.
- **`dnspop`** (low) — Use when you have a `domain` and want the most effective subdomain-guessing wordlists — returns ranked lists of common subdomain labels for 
- **`fierce-domain-scanner`** (low) — Use when you have a `domain` and want to enumerate its subdomains and map them to non-contiguous IP space — returns discovered `domain`s and
- **`gdns`** (low) — Quick DNS enumeration via Google DNS services
- **`gobuster`** (low) — Use when you have a `domain` and want to brute-force its subdomains/paths/vhosts — returns discovered subdomains, directories and virtual ho
- **`owasp-maryam`** (low) — Use when you have a `domain`, `email`, or `username` and want modular recon in one framework — returns subdomains, docs/metadata, related `e
- **`pentest-tools-com-subdomains`** (low) — Use when you have a `domain` and want a quick browser-based subdomain enumeration with no local setup — returns discovered subdomains and DN
- **`recon-ng`** (medium) — Use when you have a `domain`, `employer-org`, `email` or `ip-address` and want a scripted, modular framework to harvest hosts, subdomains, c
- **`sublist3r`** (low) — Use when you have a `domain` and want to enumerate its subdomains — returns a consolidated list of subdomains from search engines, cert data
- **`xray`** (low) — Use when you have a `domain` and want automated subdomain discovery with port/banner and Shodan enrichment — returns subdomains, open ports 

_Auto-generated by build_index.py; replace with curated prose anytime (it won't be overwritten)._
