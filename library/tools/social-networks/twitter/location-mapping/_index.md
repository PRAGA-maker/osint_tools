---
name: social-networks-twitter-location-mapping-index
description: 'Group index for tools/social-networks/twitter/location-mapping: one-line accounting of children.'
kind: group-index
---

# social-networks/twitter/location-mapping

## Skills here
- **`creepy-2`** (high) — Use when you have a `username` and want to aggregate geolocated posts into a map — but the tool is deprecated and its social-API sources are
- **`fedica`** (medium) — Use when you have a public `username`/account and want audience analytics, posting patterns, and follower-location mapping — returns `geoloc
- **`geosocial-footprint`** (high) — Use when you have a Twitter/X `username` and want a map of that account's geotagged tweets — returns plotted locations, a heatmap and a down
- **`one-million-tweet-map`** (high) — Use when you have a keyword/hashtag or `username` and want to see where recent geotagged tweets originate — returns geolocation of posts on 

_Auto-generated by build_index.py; replace with curated prose anytime (it won't be overwritten)._
