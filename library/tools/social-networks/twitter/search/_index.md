---
name: social-networks-twitter-search-index
description: 'Group index for tools/social-networks/twitter/search: one-line accounting of children.'
kind: group-index
---

# social-networks/twitter/search

## Skills here
- **`followerwonk`** (high) — Use when you have a Twitter/X `username` and want audience/bio analytics and account-overlap comparison — returns follower demographics and 
- **`tweeterid`** (high) — Use when you have an X/Twitter `username` (or numeric ID) and want the stable numeric account ID, which survives handle changes — returns th
- **`twitter-advanced-search`** (high) — Use when you have a `username`, `name` or keywords and want precisely-filtered public posts — returns tweets narrowed by account, date, loca
- **`twitter-date-search`** (high) — Use when you have a Twitter/X `username` or keyword and want tweets from a specific date window — using `since:`/`until:` search operators t
- **`twopcharts`** (medium) — Use when you have an X/Twitter `username` and want account metadata and history (creation date, name-change history, activity stats) — retur

_Auto-generated by build_index.py; replace with curated prose anytime (it won't be overwritten)._
