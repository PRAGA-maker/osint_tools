---
name: geolocation-coordinates-index
description: 'Group index for tools/geolocation/coordinates: one-line accounting of children.'
kind: group-index
---

# geolocation/coordinates

## Skills here
- **`batch-geocoding`** (medium) — Use when you have a list of UK postcodes/addresses and need to convert them all to latitude/longitude (plus admin areas, nearest station, et
- **`batch-reverse-geocoding`** (medium) — Use when you have a list of latitude/longitude coordinates and need to turn them all into the nearest UK postcode/address and admin area in 
- **`gpsvisualizer`** (high) — Use when you have raw `geolocation` coordinates, a list of `address`es, or a GPX/KML/CSV track and need to geocode, convert, and plot them o
- **`military-grid-reference-system-coordinates`** (medium) — Use when you need to convert MGRS grid references to/from lat/lon (e.g. coordinates from military or SAR sources) — returns coordinates in e

_Auto-generated by build_index.py; replace with curated prose anytime (it won't be overwritten)._
