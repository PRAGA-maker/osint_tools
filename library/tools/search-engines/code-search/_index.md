---
name: search-engines-code-search-index
description: 'Group index for tools/search-engines/code-search: one-line accounting of children.'
kind: group-index
---

# search-engines/code-search

## Skills here
- **`github-dorks`** (medium) — Use when you have a `username`, org, or `domain` and want to hunt exposed secrets/sensitive files in their GitHub repos — returns matching c
- **`gitleaks`** (medium) — Use when you have a target's git repo/`username` and want secrets and committer identities buried in its history — returns leaked credential
- **`nerdydata`** (low) — Use when you have a code snippet, tracking ID, or tech string and want every website whose source contains it — returns the matching `domain
- **`publicwww`** (medium) — Use when you have a unique code string tied to a `domain` — an analytics/AdSense ID, a tracking snippet, a wallet address, a shared template
- **`searchcode`** (medium) — Use when you have a `username`, `email` or distinctive string and want to find it in public source code — returns code files across GitHub/G

_Auto-generated by build_index.py; replace with curated prose anytime (it won't be overwritten)._
