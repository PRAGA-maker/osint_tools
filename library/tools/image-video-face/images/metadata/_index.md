---
name: image-video-face-images-metadata-index
description: 'Group index for tools/image-video-face/images/metadata: one-line accounting of children.'
kind: group-index
---

# image-video-face/images/metadata

## Skills here
- **`apache-tika`** (high) — Use when you have recovered documents, images, or media and want to extract their text and embedded metadata (EXIF, author, GPS, timestamps)
- **`c2pa-verify`** (medium) — Use when you have an image and want to read its C2PA Content Credentials — provenance, origin app, AI-generation flags, and edit history.
- **`exifeditor`** (high) — Use when you have an image file and want to read (or strip) its EXIF in the browser — returns metadata-exif including geolocation and captur
- **`exiftool`** (high) — Use when you have any media/document file and want a complete, authoritative metadata dump locally — returns metadata-exif, geolocation, dev
- **`exifviewer`** (high) — Use when you have an image (file or URL) and want a quick web read of its EXIF and GPS without a CLI — returns metadata-exif and geolocation
- **`exiv2`** (high) — Use when you need scriptable, local image-metadata parsing/editing in a pipeline — returns metadata-exif and geolocation from EXIF/IPTC/XMP.
- **`foca`** (high) — Use when you have a `domain` and want to harvest public documents from it and extract their metadata — returns usernames, emails, software/p
- **`geosetter`** (high) — Use when you need to read, map, and verify the GPS/geotag metadata across a batch of photos (and edit/correct geotags) — returns plotted loc
- **`hachoir`** (high) — Use when you have a media or binary file and want to extract embedded metadata and inspect its internal structure — a Python library/CLI ret
- **`jeffrey-s-exif-viewer`** (high) — Use when you have an image (URL or file) and want a thorough EXIF/metadata dump including GPS and camera details — returns full metadata via
- **`jpegsnoop`** (high) — Use when you have a JPEG `image` and want deep forensic analysis — returns EXIF `metadata-exif`, camera signature (`device-id`), and tamper/
- **`mediainfo`** (medium) — Use when you have a video or audio file and want its technical metadata — returns codec, container, duration, encoder tags and any embedded 
- **`metadata2go`** (high) — Use when you have an `image` or file and want its embedded metadata — returns EXIF/IPTC data including camera, timestamps and any GPS `geolo
- **`metapicz`** (high) — Use when you have an `image` (file or URL) and want its EXIF/IPTC metadata and any embedded GPS — returns camera/device `metadata-exif` and 
- **`oletools`** (medium) — Use when you have an Office document (doc/xls/ppt) and need its embedded metadata, authorship, timestamps, or to triage macros — returns aut
- **`search-by-exif`** (high) — Use when you have an image and want a fast web read of its metadata — returns EXIF fields with an emphasis on GPS/geolocation to place where
- **`xeuledoc-fetch-metadata-about-any-public-google-document`** (high) — Use when you have a public Google Doc/Sheet/Drive URL and want its owner and history — returns the creator's Google ID, creation/modificatio

_Auto-generated by build_index.py; replace with curated prose anytime (it won't be overwritten)._
