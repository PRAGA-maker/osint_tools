---
name: image-video-face-images-flickr-index
description: 'Group index for tools/image-video-face/images/flickr: one-line accounting of children.'
kind: group-index
---

# image-video-face/images/flickr

## Skills here
- **`flickr-hive-mind`** (high) — Use when you have a `username`, tag, or keyword and want to mine public Flickr photos — returns filtered photo sets, uploader profiles and s
- **`flickr-map`** (medium) — Use when you have a `geolocation` (or a Flickr `username`) and want geotagged photos there — returns public `image`s plotted on a map with t
- **`flickr`** (high) — Use when a person of interest may have a Flickr account and you want their public photos, tags, EXIF/geolocation, and account context.
- **`idgettr`** (medium) — Use when you have a Flickr username or profile URL and need the account's stable numeric NSID for API calls, feeds, or de-duplicating the ac
- **`my-pics-map`** (high) — Use when you want to plot a Flickr user's geotagged photos on a map to infer locations they frequent — returns mapped geolocation points; se

_Auto-generated by build_index.py; replace with curated prose anytime (it won't be overwritten)._
