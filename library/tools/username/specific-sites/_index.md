---
name: username-specific-sites-index
description: 'Group index for tools/username/specific-sites: one-line accounting of children.'
kind: group-index
---

# username/specific-sites

## Skills here
- **`amazon-usernames`** (high) — Use when you have a username or name and want to find the subject's Amazon public profile, reviews, wish lists or lists — returns Amazon pro
- **`github-user`** (high) — Use when you have a GitHub `username` and want their recent public activity — returns a JSON feed of pushes, PRs, issues, and comments with 
- **`keybase`** (high) — Use when you have a `username` and want to pivot to the same person's verified other accounts, PGP key and crypto wallets — returns cross-pl
- **`mit-pgp-key-server`** (high) — Use when you have an `email`, `name`, or key ID and want to find a subject's published PGP public key and the identities (UIDs/emails) attac
- **`protonmail-domains`** (high) — Use when you have an `email` (on any domain) and want to know whether it is hosted on Proton Mail — the public HKP key-lookup returns a PGP 
- **`protonmail-users`** (high) — Use when you have a candidate Proton (ProtonMail) `email`/`username` and want to confirm it exists — query Proton's public PGP key server an
- **`tinder-usernames`** (high) — Use when you have a Tinder `username` and want to confirm the profile exists and see its public web card — returns name, photos and basic in

_Auto-generated by build_index.py; replace with curated prose anytime (it won't be overwritten)._
